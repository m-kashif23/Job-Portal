spring.application.name=springapp

# --- MySQL ---
spring.datasource.url=jdbc:mysql://localhost:3306/jobportal?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD:Kashif@123}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# --- Quick test with H2 instead (also uncomment H2 in pom.xml) ---
#spring.datasource.url=jdbc:h2:mem:jobportal
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=
#spring.jpa.hibernate.ddl-auto=update

# --- JWT (change the secret; ideally set via environment variable JWT_SECRET) ---
jwt.secret=${JWT_SECRET:Xk9pQ2vT7mR4nL8wF3jH6dS1aZ5cB0yE9uG2iO7pK4tM8rN3qW6vJ1xD5hA0fL9s}
jwt.expiration-ms=1800000

# --- Bootstrap admin account (created on first run if missing) ---
app.admin.email=admin@jobportal.com
app.admin.password=Admin@123

# --- CORS: comma-separated list of allowed frontend origins ---
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200}

# --- Resume uploads ---
app.resume.storage-dir=${RESUME_STORAGE_DIR:C:/Users/kashi/OneDrive/Desktop/Job2/Resume}
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
